fn cost(w) {
    return w * w;
}

fn main() {
    let inputs = [1, 2, 3];
    let weights = [0.1, 0.2, 0.3];
    let dot = inputs @ weights;
    print(dot);

    let d = grad(cost)(2);
    print(d);

    let nested = [[1, 0], [0, 1]];
    print(nested);
}
